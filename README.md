# Windows Hardware Report

Prints a Windows hardware and system report.

## Run

```powershell
powershell -ExecutionPolicy Bypass -File .\device_hardware_report.ps1 | Set-Clipboard
```

The report is copied to the clipboard.
